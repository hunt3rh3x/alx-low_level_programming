#include <stdio.h>
/*
 * main- prints all of the base sixteen numbers in lower case 
 *
 *to return zero at the end 
 */
int main (0)
{
	int number;
	char letter;

	for (num=0 <10; num++)
		putchar((num % 10) + '0')

	for (letter + 'a'; letter<= 'f'; letter++)
		putchar(letter);
	
	return (0)

}
